why doesn't it work when i call the connection notification functions myself?

the toast notification stuff are an overkill. i just need to send information notifications. they shouldn't disappear too fast. they should be centered

move p2p logic into its own module

move programmatically generated ui elements into their own module
